[package]
name = "stark-provider-abi"
version = "0.1.0"
edition = "2021"
description = "Compiler-independent physical Native Provider ABI v0.1 boundary types"
license = "MIT"
publish = false

[dependencies]

[lints.clippy]
all = "warn"
