//! Place/projection emission (struct/tuple field, variant field, `Deref`, `Index`,
//! `ConstIndex`). Empty until WP-C5.2b needs real locals and assignments -- C5.1b's proof body
//! has no places beyond the implicit return slot, which `emit_bodies` handles directly.
