use lib::scaled;
use lib::Counter;
use lib::bump;

fn main() {
    let mut total: Int32 = 0;
    let mut i: Int32 = 0;
    while i < 100 { total = total + scaled(i); i = i + 1; }
    let c: Counter = bump(Counter { n: 41 });
    print(total);
    print("|");
    print(c.n);
}
