fn first(values: &[Int32]) -> Int32 { values[0] }
fn read(value: &Int32) -> Int32 { *value }
fn main() { let mut value = 3; let copied = read(&mut value); let values = [1, 2]; let head = first(&values); let widened = head as Int64; }
