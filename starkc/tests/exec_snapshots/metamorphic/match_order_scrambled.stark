fn label(n: Int32) -> String {
    match n {
        3 => String::from("three"),
        1 => String::from("one"),
        2 => String::from("two"),
        _ => String::from("other"),
    }
}

fn main() {
    println(label(1).as_str());
    println(label(2).as_str());
    println(label(3).as_str());
    println(label(9).as_str());
}
