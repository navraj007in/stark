fn main() {
    let mut m: HashMap<Int32, Int32> = HashMap::new();
    m.insert(30, 0);
    m.insert(10, 1);
    m.insert(20, 2);
    assert_eq(m.len(), 3);
    for k in m.keys() {
        print(*k);
    }
}
