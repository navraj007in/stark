fn main() {
    let mut m: HashMap<Int32, Int32> = HashMap::new();
    m.insert(1, 0);
    m.insert(2, 1);
    m.insert(3, 2);
    assert_eq(m.len(), 3);
    for k in m.keys() {
        print(*k);
    }
}
