# §11.11 retention record for DEV-117. Written when the defect was found, kept after it is fixed.
dev_id = "DEV-117"
status = "open"
found_by = "r07__ownership_copy_move (R-07 attribution, CD-171)"
normative_rule = "OWN-REINIT-001"
first_differing_field = "completion versus trap"
summary = "reinitialising a moved-from local is accepted by the front end and RUNS in the HIR oracle, but MIR verification refuses it with MIR-0007 `move from possibly-moved place` -- the move analysis never clears the moved flag on reassignment"
hir_oracle = "completes"
mir = "refused at verification: MIR-0007"
native_debug = "not reached -- the program never lowers"
generator_seed = "n/a -- handwritten case, not generated"
template_id = "n/a"
dimensions = "n/a"
original_sha256 = "recorded by the retention test from cases/retained/DEV-117/original/main.stark"
reduced_sha256 = "recorded by the retention test from cases/retained/DEV-117/reduced/main.stark"
reduction_equivalent = "yes -- both reproduce MIR-0007 on the same local; the original is kept until the reduction is proven equivalent by the fix landing (§11.11 step 5)"
manifest_entry = "withheld while `status = open`: the case cannot lower, so adding it to the runnable manifest would fail the replay. §11.11 step 6 applies when the defect is fixed and the reduced case becomes a regression."
