# §11.11 retention record for DEV-116. Written when the defect was fixed; kept as a regression.
dev_id = "DEV-116"
status = "fixed"
found_by = "c63d_map_key_identity::hashset_is_hir_only (V19), raised to a blocker in CD-169"
normative_rule = "STD-HASH-001"
first_differing_field = "completion versus trap"
summary = "`HashSet` is normative in the admitted `std-full` surface and the HIR oracle ran it, but MIR lowering refused the TYPE outright (`type Core(HashSet, [Primitive(Int32)]) (C4.5)`) -- a MIR/native lowering gap, which §4.3 forbids recording as a non-Core exclusion"
hir_oracle = "completed"
mir = "refused at lowering; fixed CD-176"
native_debug = "not reached -- the program never lowered; now builds and runs"
generator_seed = "n/a -- handwritten case, not generated"
template_id = "n/a"
dimensions = "n/a"
original_sha256 = "recorded by the retention test from cases/retained/DEV-116/original/main.stark"
reduced_sha256 = "recorded by the retention test from cases/retained/DEV-116/reduced/main.stark"
reduction_equivalent = "yes -- both exercise construct/insert/len over the same element type; the original additionally covers remove, contains, clear and is_empty, which are kept as their own corpus case rather than folded in here"
manifest_entry = "dev116__reinstated_hashset -- §11.11 step 6, added when the defect was fixed"
