fn main() {
    let value: Option<Option<Int32>> = Some(None);
    let out: Int32 = match value {
        Some(inner) => match inner {
            Some(v) => v,
            None => -1,
        },
        None => -2,
    };
    assert_eq(out, -1);
    print(out);
}
