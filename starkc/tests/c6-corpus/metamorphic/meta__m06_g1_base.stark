fn main() {
    let value: Option<Option<Int32>> = Some(Some(5));
    let out: Int32 = match value {
        Some(inner) => match inner {
            Some(v) => v,
            None => -1,
        },
        None => -2,
    };
    assert_eq(out, 5);
    print(out);
}
