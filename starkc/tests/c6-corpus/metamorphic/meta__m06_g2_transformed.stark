fn main() {
    let value: Option<Option<Int32>> = Some(None);
    let inner: Option<Int32> = match value {
        Some(inner) => inner,
        None => None,
    };
    let out: Int32 = match inner {
        Some(v) => v,
        None => -1,
    };
    assert_eq(out, -1);
    print(out);
}
