enum Colour { Red, Green, Blue }
fn main() {
    let c: Colour = Colour::Blue;
    let code: Int32 = match c {
        Colour::Blue => 3,
        Colour::Green => 2,
        Colour::Red => 1,
    };
    assert_eq(code, 3);
    print(code);
}
