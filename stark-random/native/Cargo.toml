[package]
name = "stark-random-native"
version = "0.1.0"
edition = "2021"
description = "Native secure randomness provider for STARK Native Provider ABI v0.1"
license = "MIT"
publish = false

[dependencies]
stark-provider-abi = { path = "../../starkc/stark-provider-abi" }

[dev-dependencies]
starkc = { path = "../../starkc" }
stark-runtime = { path = "../../starkc/stark-runtime" }
